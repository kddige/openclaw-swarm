#!/usr/bin/env bash
set -eo pipefail

for (( i=0; i <= $#; i++ )); do
  declare -x "ARG$i"="${!i}"
done

# Automatically generated by moon. DO NOT MODIFY!
# https://moonrepo.dev/docs/guides/vcs-hooks

moon run root:format-staged -- $(git diff --cached --name-only --diff-filter=ACMR) && git update-index --again
moon run :lint :typecheck --affected --status=staged

